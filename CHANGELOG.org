#+TITLE: Changelog

This file records completed changes.

* Unreleased
:PROPERTIES:
:VISIBILITY: all
:END:

- =ADDED= It should suggest a commit message.
- =REMOVED= ai-notes is not needed.
- =CHANGED= Refactored the file to be more precise and shorter.
- =ADDED= The the AI to store the plan in the task file, not only have it in the chat.
- =ADDED= AI forgot to escpape headings in org source blocks.
- =CHANGED= Even better to not move the task in the todo, just move the body to the task file.
- =CHANGED= After completion the body of the todo is moved to the task file to not clutter the todo file. Made a new template and adapted AGENTS.md.
- =ADDED= New TODO keywords: PLAN and BUILD.
- =CHANGED= AGENTS.md is much shorter now.
- =CHANGED= Template is much shorter now.
- =CHANGED= New template to go in hand with the new workflow (WIP)
- =ADDED= Model suitability and cost awareness: AI should actually pause if the model is not suitable so the user can decide. 
- =ADDED= Model suitability and cost awareness: AI should notify if model is not 
- =CHANGED= Clarified some instructions.
- =ADDED= AI task to move the task in todo.md to ~Completed~ after completing the task.

* Instructions
:PROPERTIES:
:VISIBILITY: folded
:END:

** Changelog Entry Types

- =ADDED= :: Use for new features, new functionality, or newly introduced behavior.
- =CHANGED= :: Use for modifications to existing behavior.
- =FIXED= :: Use for bug fixes or incorrect behavior that now works correctly.
- =REMOVED= :: Use for removed functionality, deleted features, or deprecated behavior that no longer exists.

** Rules

- Use one bullet per change.
- Keep entries short and clear.
- Write for users, not developers.
- Do not include unfinished work.
- Do not include internal refactorings unless they affect users.
- Add newest entries at the top of the =Unreleased= section.
- Use uppercase entry types:
  - =ADDED=
  - =CHANGED=
  - =FIXED=
  - =REMOVED=
- Keep wording simple and direct.
- Prefer describing the visible result instead of implementation details.

** Releases

When creating a release:
- move entries from =Unreleased= into a version section
- keep the same bullet format
- use the following heading format:
  #+begin_src org
    ,* 0.1.0 - YYYY-MM-DD
  #+end_src

** Example Release

#+begin_src org
  ,* 0.1.0 - 2026-05-22

  - =ADDED= Added CSV export for Anki notes via AnkiConnect.
  - =CHANGED= Clarified AI workflow and commit rules.
  - =FIXED= Fixed incorrect WAIT status handling.
  - =REMOVED= Removed outdated setup instructions.
#+end_src
